@{
    ViewData["Title"] = "Assign Roles";
}

<h1>Assign Roles</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Assign role to user</a>
</p>

<p class="text-muted">
    This page can later list which users have which roles. For now it just exists so redirects work.
</p>